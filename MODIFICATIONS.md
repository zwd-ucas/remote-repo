# 修改说明 / Modifications

本仓库基于上游开源项目 **pyVideoTrans**（https://github.com/jianchang512/pyvideotrans ，GPL‑3.0）二次修改而来。
This repository is a modified copy of the upstream **pyVideoTrans** project (GPL‑3.0). Changes are stated below as required by the GPL.

## 新增功能：SRT 行内说话人标记 → 自动逐行配音音色

让带说话人标记的字幕**在配音时自动按标记里的"音色"逐行分配声音**，无需在弹窗里手动为每个说话人/每一行指定音色。

### 字幕格式
每条字幕正文开头写 `[角色-音色]`（也支持中文方括号 `【角色-音色】`）：

```srt
1
00:00:06,740 --> 00:00:13,690
[旁白-沧明子] 这是灰姑娘的故事

2
00:00:18,800 --> 00:00:21,250
[灰姑娘-少女阿月] 我也可以去参加舞会吗

3
00:00:24,800 --> 00:00:27,220
[继母-卡捷琳娜] 只有等你打扫完厨房
```

- 方括号里**连字符后面的部分是"音色"**（前面的"角色"只是给人看的，可随意写）。
- "音色"必须是所选 TTS 的音色名。对 **Qwen3‑TTS**，用中文音色名即可，例如 `沧明子 / 凯 / 苏瑶 / 卡捷琳娜 / 诡婆婆 / 十三 / 少女阿月 / 萌宝 / 顽屁小孩 …`（程序会自动匹配到 `videotrans/voicejson/qwen3tts.json` 里的完整名 `沧明子(Eldric Sage)` 并传给接口）。
- 命中已知音色后，标记会**从朗读文本里删掉**（不会被念出来），并**回写成干净字幕**（烧录/显示也不含标记）。
- 没匹配到任何已知音色的方括号会**原样保留**，不影响普通字幕。

### 用法
1. 配音引擎选 **Qwen3‑TTS**（或其它音色名能对上的引擎）。
2. 把带 `[角色-音色]` 标记的 SRT 作为目标字幕导入/翻译产出。
3. （可选）把"字幕编辑倒计时"设为 0，跳过手动分配弹窗；直接开始配音即可逐行用对的音色。

### 音色优先级
`字幕行内标记` > `弹窗逐行设置(line_roles)` > `全局音色`
（即标记没匹配上时，完全退回原版行为，向后兼容。）

## 代码改动
仅改动一个文件：**`videotrans/task/trans_create.py`**
- 新增方法 `TransCreate._parse_inline_voice_tags(self, subs)`：用 `tools.role_menu(tts_type)` 取当前引擎音色清单，按"括号前中文名"建立索引，正则解析每行行首 `[..]/【..】`，取能作为后缀的最长音色名（兼容 `上海-阿珍` 这类含连字符的方言名）；命中则记录 `{行号: 音色完整名}`、就地去除标记文本、并回写干净 `target_sub`。
- 在 `TransCreate._tts()` 配音预处理里调用该方法，并把每行音色解析为：
  `voice = inline_roles.get(行号) or line_roles.get(行号, 全局音色)`。

未匹配音色、或未使用标记的字幕，行为与上游完全一致。

## 许可证
继续遵循上游 **GPL‑3.0**，完整许可证见 `LICENSE`。本修改同样以 GPL‑3.0 提供。
